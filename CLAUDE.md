# CLAUDE.md — Референс для сайта-визитки Kamil (cloofen)

Этот файл — основной ориентир при разработке сайта. Все решения по дизайну, анимации и структуре должны согласовываться с тем, что написано здесь.

---

## О проекте

- **Тип:** Сайт-визитка / портфолио CGI Artist
- **Цель:** Показать работы клиентам, получить заказы
- **Аудитория:** Потенциальные клиенты — русскоязычные и иностранные
- **Язык интерфейса:** английский (заказчики есть и за рубежом)
- **Прод:** https://cloofen-site.vercel.app/ — Vercel, автодеплой из `main` на GitHub (`tagirov3322-blip/cloofen-site`)

## Стек

Чистый HTML/CSS/JS в одном файле `index.html`. Без сборки, без зависимостей, без npm.
Шрифты: `Share Tech Mono` с Google Fonts + локальный `OffBit-Bold.ttf` для имени.

---

## Контент — шапка

Текста ровно столько, сколько ниже. Всё остальное — знакомство, условия, опыт —
идёт в личных сообщениях с заказчиком, на сайте этого быть не должно.

- **Имя:** Kamil
- **Возраст:** 20
- **Специализация:** CGI Artist
- **Софт:** Unreal Engine, Blender, After Effects
- **Telegram:** @cloofenm → ссылка `https://t.me/cloofenm`
- **Ни фото автора, ни логотипа на странице нет** — оба съедали место, которое нужно работам.

Описания, слоганов и «available for freelance» не добавлять.

---

## Общая стилистика

- **Настроение / атмосфера:** Терминальная / ретро-хакерская / минималистичная. Ощущение старого монитора, командной строки. Сурово, лаконично, никакой лишней декорации.
- **Ориентир:** Сайт Yannick Gregoire (yannickgregoire.nl) — структура, типографика, цветовая схема взяты оттуда как база.
- **Чего точно НЕ хочется:** Корпоративный стиль, кислотные цвета, перегруженность, 3D-пузыри и «красивые» градиенты в духе Dribbble.

---

## Цвета

- **Основной фон:** `#000000` — чистый чёрный
- **Текст:** `#ffffff` — белый
- **Акцентный цвет:** `#00ffa8` — холодный зелёный
- **Приглушённый текст:** `#555` (метки секций), `#888`/`#aaa` (мета и описание)
- **Дополнительные цвета:** не нужны — вся система держится на чёрном, белом и одном акценте

---

## Типографика

- **Шрифт:** Моноширинный — `Share Tech Mono`, fallback `ui-monospace, 'Courier New', monospace`
- **Имя в шапке:** `OffBit-Bold.ttf`, uppercase, `clamp(26px, 2.8vw, 38px)` — намеренно скромное
- **Иерархия шапки:** главный акцент — кнопка Telegram, но акцент держится **размером**
  (16px, крупные отступы), а не заливкой. В покое это контур, заливка появляется на hover —
  как и у всех остальных ховеров на сайте. Софт и возраст — 14px. Имя мельче обоих:
  цель страницы — чтобы написали, а не чтобы прочитали имя покрупнее.
- **Заголовки:** Крупные, всё заглавными, разреженный трекинг
- **Стиль написания:** мелкий текст — строчными с префиксом `/ ` в духе терминала
- **Line-height:** Плотный — около `1.2em`

---

## Анимации и интерактивность

- **Общий тон:** Минимальные анимации. Если есть — резкие, без «ease-in-out» мыльности.
- **Вход:** стаггер `fadeUp` — шапка 0.15s, подсказка 0.30s, сетка 0.40s
- **Эффекты при наведении:** инверсия цвета или акцентная рамка — без scale и теней
- **Фон:** canvas с бинарным шумом (`0`/`1`, `rgba(0,60,30,…)`), перерисовка на resize через `requestAnimationFrame`
- **Фоновое свечение:** одно радиальное пятно сверху по центру, без смены состояний
- **Клавиатура:** карточки — `<button>`, стрелки ходят по сетке, Enter открывает лайтбокс, Esc закрывает

---

## Структура

Сайт — **одна секция на один экран**. Ни скролла на десктопе, ни подстраниц.
Главное на странице — работы; шапка занимает минимум места.

```
/ CGI Artist   ← абсолютом в левом верхнем углу страницы, вне потока шапки

KAMIL
AGE — 20
SOFTWARE — UNREAL ENGINE, BLENDER, AFTER EFFECTS   [ / WRITE ON TELEGRAM ]

/ hover to expand — click to play
┌────────┐ ┌────────┐ ┌────────┐
│   01   │ │   02   │ │   03   │
└────────┘ └────────┘ └────────┘
┌────────┐ ┌────────┐ ┌────────┐
│   04   │ │   05   │ │   06   │
└────────┘ └────────┘ └────────┘
```

Сетка **3×2, шесть работ**. При наведении колонка и ряд карточки растягиваются
(`1.7fr / 0.65fr` по колонкам, `1.45fr / 0.55fr` по рядам), остальные уходят в
`brightness(0.3) blur(2px)`. Подпись — название + строка описания акцентным цветом.
Клик — лайтбокс с полноразмерным видео и звуком.

Работы по порядку: Byreal · Solayer · Mr. Stephen · Sentient · Seismic · Checkout.

Высота сетки считается из ширины ячейки (`--cell * 0.5625 * 2 + gap`), чтобы
в покое плитки были ровно 16:9, а `fr`-анимация раскрытия шла по фиксированной
коробке. Ниже 861px раскрытие отключается — сетка становится 2 колонки, затем 1.

---

## Медиа — обязательный конвейер

Исходники лежат в `C:\Users\Shambooo\Videos\Portfolio` и в репозиторий **не кладутся**
(4K, 15–40 МБ штука). В `media/` попадают только пережатые версии:

| Файл | Что это | Параметры |
|---|---|---|
| `<slug>-preview.mp4` | видео в сетке | 960w, без звука, `-crf 28`, `+faststart` (~0,3–0,8 МБ) |
| `<slug>.mp4` | видео в лайтбоксе | 1920w, звук aac 128k, `-crf 22`, `+faststart` (~3–10 МБ) |
| `<slug>.jpg` | постер карточки | кадр с 2-й секунды, 960w, `-q:v 5` |

```bash
ffmpeg -ss <старт> -t <длина> -i src.mp4 -an -vf "scale=960:-2" -c:v libx264 -crf 28 \
  -preset slow -profile:v main -pix_fmt yuv420p -movflags +faststart media/<slug>-preview.mp4
ffmpeg -i src.mp4 -map 0:v -map "0:a?" -vf scale=1920:-2 -c:v libx264 -crf 22 \
  -preset medium -pix_fmt yuv420p -c:a aac -b:a 128k -movflags +faststart media/<slug>.mp4
ffmpeg -ss 2 -i src.mp4 -frames:v 1 -vf scale=960:-2 -q:v 5 media/<slug>.jpg
```

**Правила загрузки:**
- У каждого `<video>` в сетке обязателен `poster` — иначе до буферизации карточка чёрная
- `preload="metadata"` в разметке; на десктопе JS поднимает до `auto` и запускает все превью
- На мобильных превью играет **только видимое** (IntersectionObserver, threshold 0.5) — шесть одновременных декодов сажают батарею
- Полноразмерное видео грузится **только по клику** в лайтбоксе, при закрытии `src` снимается

---

## SEO / шеринг

Ссылку кидают в Telegram и в личку, поэтому в `<head>` обязаны быть:
`description`, `og:title`, `og:description`, `og:image` (`og.jpg`, 1200×630), `twitter:card`, favicon.
При смене домена обновить абсолютные URL в og-тегах.

---

## Референсы

- **Yannick Gregoire** (yannickgregoire.nl) — главный визуальный ориентир: терминальная монохромная типографика, чёрный фон, зелёный акцент, навигация с клавиатуры.

---

## Заметки

- Оригиналы работ и полный архив: `C:\Users\Shambooo\Videos\Portfolio`
- В ролике Seismic на титре опечатка: «The Encrypted Foundtaion» вместо *Foundation*. Вшита в видео, видна и в плитке, и в лайтбоксе — чинится только перерендером титра.
- Страницу намеренно не расширяем: ни «обо мне», ни услуг, ни отзывов. Всё это Kamil проговаривает в личке с заказчиком.
